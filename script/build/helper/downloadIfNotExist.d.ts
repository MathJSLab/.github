/**
 * Downloads the file if it does not exist. Recursively creates the base
 * directories if necessary.
 * @param url
 * @param filePath
 */
export default function (url: string, filePath: string): Promise<boolean | void>;
